// Placeholder for @computerworks/ui
// The actual SPA entry point is `src/main.tsx`; this module re-exports
// the public API surface so the workspace's tsc -b composite build
// still has something to emit. The browser never imports this file.
export const UI_VERSION = "0.0.1";
